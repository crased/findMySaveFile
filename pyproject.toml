[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "findsavegame"
version = "0.1.0"
dependencies = []

[project.scripts]
findmysave = "main:main"

[tool.setuptools]
py-modules = ["main", "search_scripts", "pattern_scripts"]